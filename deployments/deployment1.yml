apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: nginxnpc
spec:
  replicas: 5
  template:
    metadata:
      labels:
        team: green
        type: npc
    spec:
      containers:
        - image: tourna/nginx:v1
          name: nginx
