apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: nginxnpc
spec:
  replicas: 4
  template:
    metadata:
      labels:
        team: green
        type: npc
    spec:
      containers:
        - image: tourna/nginx:v1
          name: nginx
